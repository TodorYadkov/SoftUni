<section class="catalog" id="catalog">
    <h2>Book Reviews</h2>
    <div class="container">
        {{#each allBooks}}
        {{>bookCard}}
        {{else}}
        <h2 class="no-record">There are no book reviews found yet...</h2>
        {{/each}}
    </div>
</section>